name: Workflow in Repo1

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run build steps
        run: echo "First repo workflow completed successfully"
